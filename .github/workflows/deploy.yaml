name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t sxsurapaneni/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true sxsurapaneni/react-test npm test

      - run: docker build -t sxsurapaneni/multi-client ./client
      - run: docker build -t sxsurapaneni/multi-nginx ./nginx
      - run: docker build -t sxsurapaneni/multi-server ./server
      - run: docker build -t sxsurapaneni/multi-worker ./worker

      - run: docker push sxsurapaneni/multi-client
      - run: docker push sxsurapaneni/multi-nginx
      - run: docker push sxsurapaneni/multi-server
      - run: docker push sxsurapaneni/multi-worker